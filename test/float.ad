fn main() {
    0.0;
    1.1;
    12.34;
    123.456;
    -2.9;
    3.1415926535897932;  // 3.141592653589793まで

    assert_eq!( 1.0, { let a: f32 =  1.0; a });
    assert_eq!(-1.0, { let a: f32 = -1.0; a });
    assert_eq!( 2.2, 2.2);

    // TODO: `float64`から`float32`への`unbox`は出来ないからどうするか…
    //assert_eq!( 1.0, { let a: Box<f32> = Box::new(1.0); *a });

    println!(12.34);
    println!({ let a: f32 = 12.34; a });

    // should compile error
    //let a: f32 = 1;
    //let a: i32 = 1.0;

    println!("ok");
}
